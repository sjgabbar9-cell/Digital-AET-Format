# AET QPDC Dashboard

Upload the complete folder contents to GitHub. Data is stored in `data/targets.csv` and `data/daily_entries.csv`, which open in Excel as spreadsheets. Deploy `app.py` on Streamlit Community Cloud and add the values from `secrets.toml.example` under App Settings > Secrets. Do not upload a real secrets file.
